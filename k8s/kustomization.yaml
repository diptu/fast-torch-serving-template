apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  # secret.yaml is gitignored and not listed here — apply it separately:
  #   cp secret.example.yaml secret.yaml   # fill in real values
  #   kubectl apply -f secret.yaml
